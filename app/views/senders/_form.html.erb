<%= form_with(model: sender, local: true) do |form| %>
  <% if sender.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sender.errors.count, "error") %> prohibited this sender from being saved:</h2>

      <ul>
      <% sender.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <table class="table table-striped">
    <thead>
    </thead>

    <tbody>
      <tr>
        <th><%= form.label :name %></th>
        <td><%= form.text_field :name %></td>
      </tr>

      <tr>
        <th><%= form.label :telephone %></th>
        <td><%= form.text_field :telephone %></td>
      </tr>

      <tr>
        <th><%= form.label :email %></th>
        <td><%= form.text_field :email %></td>
      </tr>

      <tr>
        <th><%= form.label :address %></th>
        <td><%= form.text_field :address %></td>
      </tr>

      <tr>
        <th><%= form.label :latitude %></th>
        <td><%= form.text_field :latitude %></td>
      </tr>

      <tr>
        <th><%= form.label :longitude %></th>
        <td><%= form.text_field :longitude %></td>
      </tr>

      <tr>
        <th><%= form.label :status %></th>
        <td><%= form.label @sender.status %>
            <%= form.hidden_field :status %></td>
      </tr>

    </tbody>
  </table>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
