<%= form_with(model: company, local: true) do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="table table-striped">
    <thead>
    </thead>

    <tbody>
      <tr>
        <th><%= form.label :name %></th>
        <td><%= form.text_field :name %></td>
      </tr>

      <tr>
        <th><%= form.label :telephone %></th>
        <td><%= form.text_field :telephone %></td>
      </tr>

      <tr>
        <th><%= form.label :email %></th>
        <td><%= form.text_field :email %></td>
      </tr>

      <tr>
        <th><%= form.label :address %></th>
        <td><%= form.text_field :address %></td>
      </tr>

      <tr>
        <th><%= form.label :latitude %></th>
        <td><%= form.text_field :latitude %></td>
      </tr>

      <tr>
        <th><%= form.label :longitude %></th>
        <td><%= form.text_field :longitude %></td>
      </tr>

      <tr>
        <th><%= form.label :radius %></th>
        <td><%= form.number_field :radius %></td>
      </tr>

      <tr>
        <th><%= form.label :status %></th>
        <td><%= form.select :status, Company.statuses.keys[0..1].map {|status| [status.titleize,status]} %></td>
      </tr>

    </tbody>
  </table>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
